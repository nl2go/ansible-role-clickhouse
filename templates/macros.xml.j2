<?xml version="1.0"?>
<!-- {{ ansible_managed }} -->
<yandex>
    <macros>
        <cluster>{{ clickhouse_display_name | default('clickhouse') }}</cluster>
        {% if node_hostname in groups['clickhouse''] %}
        <shard>01</shard>
        {% endif %}
        {% if node_hostname in groups['clickhouse''] %}
        <shard>02</shard>
        {% endif %}
        <replica>{{ inventory_hostname }}</replica>
    </macros>
</yandex>
